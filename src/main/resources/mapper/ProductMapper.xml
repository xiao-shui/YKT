<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ProductMapper">
    
    <resultMap id="BaseResultMap" type="org.example.entity.Product">
        <id column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="category" property="category"/>
        <result column="brand" property="brand"/>
        <result column="description" property="description"/>
        <result column="base_price" property="basePrice"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        product_id, product_name, category, brand, description, base_price, status, create_time, update_time
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM products
        <where>
            <if test="productName != null and productName != ''">
                AND product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="brand != null and brand != ''">
                AND brand = #{brand}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM products
        <where>
            <if test="productName != null and productName != ''">
                AND product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="brand != null and brand != ''">
                AND brand = #{brand}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM products
        WHERE product_id = #{productId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO products (product_name, category, brand, description, base_price, status)
        VALUES (#{productName}, #{category}, #{brand}, #{description}, #{basePrice}, #{status})
    </insert>

    <update id="update">
        UPDATE products
        <set>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="category != null">category = #{category},</if>
            <if test="brand != null">brand = #{brand},</if>
            <if test="description != null">description = #{description},</if>
            <if test="basePrice != null">base_price = #{basePrice},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE product_id = #{productId}
    </update>

    <delete id="deleteById">
        DELETE FROM products WHERE product_id = #{productId}
    </delete>

</mapper>

